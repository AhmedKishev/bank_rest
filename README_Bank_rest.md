<h1>🚀 Система управления банковскими картами</h1>

<h2>📁 Структура проекта</h2>
<p>
Проект организован по принципу разделения ответственности:
</p>
<ul>
<li><code>controller/</code> — REST API</li>
<li><code>service/</code> — бизнес-логика</li>
<li><code>repository/</code> — работа с БД</li>
<li><code>model/</code> — сущности</li>
<li><code>security/</code> — аутентификация и авторизация</li>
<li><code>config/</code> — конфигурации (включая Swagger)</li>
<li><code>db/migration/</code> — Liquibase-миграции</li>
<li><code>docs/</code> — OpenAPI спецификация</li>
<li><code>test/</code> — unit и integration тесты</li>
</ul>
<p>
После завершения разработки все временные файлы вроде <code>README Controller.md</code> были удалены, чтобы не попадать в итоговую сборку.
</p>

<h2>✅ Возможности</h2>
<h3>👤 Пользователь (<code>USER</code>)</h3>
<ul>
<li>Просмотр своих карт (с пагинацией и поиском)</li>
<li>Запрос на блокировку своей карты</li>
<li>Перевод средств между своими картами</li>
<li>Просмотр баланса (номер карты отображается в маске: <code>**** **** **** 1234</code>)</li>
</ul>

<h3>👨‍💼 Администратор (<code>ADMIN</code>)</h3>
<ul>
<li>Создание, активация, блокировка и удаление любых карт</li>
<li>Управление пользователями</li>
<li>Просмотр всех карт в системе</li>
</ul>

<h2>🔐 Безопасность</h2>
<ul>
<li>Аутентификация и авторизация через <strong>JWT</strong></li>
<li>Ролевой доступ: <code>USER</code> / <code>ADMIN</code></li>
<li>Все чувствительные данные (например, номер карты) <strong>шифруются</strong> в БД</li>
<li>Номера карт <strong>маскируются</strong> при выводе в API</li>
<li>Валидация входных данных и обработка ошибок</li>
</ul>

<h2>🗄️ База данных</h2>
<ul>
<li>Поддержка <strong>PostgreSQL</strong></li>
<li>Миграции управляются через <strong>Liquibase</strong><br />
Расположение: <code>src/main/resources/db/migration</code></li>
</ul>

<h2>📡 API</h2>
<ul>
<li>Полный CRUD для карт</li>
<li>Эндпоинт для перевода между своими картами</li>
<li>Поддержка фильтрации, сортировки и пагинации</li>
<li>Чёткие сообщения об ошибках (в формате JSON)</li>
</ul>
<p>
Документация API доступна через <strong>Swagger UI</strong> после запуска приложения:<br />
→ <code>http://localhost:8080/swagger-ui.html</code>
</p>
<p>
OpenAPI-спецификация: <code>docs/openapi.yaml</code>
</p>

<h2>Запуск</h2>
<h3>Требования</h3>
<ul>
<li>Java 17+</li>
<li>Docker &amp; Docker Compose (для dev-среды)</li>
</ul>

Приложение будет доступно на порту 8080
</code></pre>
<h3>Конфигурация</h3>
<p>
Настройки подключения к БД, JWT и другие параметры — в <code>application.yml</code>.
</p>

<h2>🧪 Тестирование</h2>
<ul>
<li>Юнит-тесты покрывают ключевую бизнес-логику</li>
<li>Integration-тесты для контроллеров</li>
</ul>
<p>

<h2>🛠️ Технологии</h2>
<p>
Java 17+, Spring Boot, Spring Security + JWT, Spring Data JPA, PostgreSQL / MySQL, Liquibase, Docker / Docker Compose, Swagger (OpenAPI)
</p>